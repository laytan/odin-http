name: CI
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: 0 20 * * *

env:
  FORCE_COLOR: "1"

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2022, macos-14] # macos-latest is intel, 14 is arm
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - uses: laytan/setup-odin@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Report
        run: odin report
      - name: Non-blocking IO test
        run: odin test nbio
        timeout-minutes: 1
      - name: Odin check
        if: success() || failure()
        run: odin check examples/complete -vet --strict-style && odin check examples/client -vet --strict-style
        timeout-minutes: 1
